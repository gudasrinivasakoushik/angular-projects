import { FilterfilterPipe } from './filterfilter.pipe';

describe('FilterfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
